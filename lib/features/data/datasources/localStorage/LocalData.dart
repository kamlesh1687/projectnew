import 'package:projectnew/features/data/models/UserProfileModel.dart';

class LocalDataService {
  deleteUserData() {
    try {} catch (e) {}
    throw UnimplementedError();
  }

  Future<UseR> loadUserData() {
    try {} catch (e) {}

    throw UnimplementedError();
  }

  saveUserData(UseR _user) {
    try {} catch (e) {}

    throw UnimplementedError();
  }
}
